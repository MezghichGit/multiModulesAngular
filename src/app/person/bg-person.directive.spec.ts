import { BgPersonDirective } from './bg-person.directive';

describe('BgPersonDirective', () => {
  it('should create an instance', () => {
    const directive = BgPersonDirective;
    expect(directive).toBeTruthy();
  });
});
